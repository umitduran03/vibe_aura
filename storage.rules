rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      // Yüklemeler sadece kimliği doğrulanmış anonim veya kalıcı üyelere açık
      allow write: if request.auth != null;
    }
  }
}
